[build]
  # No build needed - this is a static HTML/CSS site
  command = "echo 'No build needed - static site'"
  # Publish from root directory where HTML files are
  # Use explicit path to avoid confusion
  base = "."
  publish = "."

[build.environment]
  # Use Node 18 LTS for compatibility
  NODE_VERSION = "18"

