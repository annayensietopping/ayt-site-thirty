[build]
  # No build needed - this is a static HTML/CSS site
  # Empty command tells Netlify to skip build
  command = "true"
  publish = "."

[build.environment]
  # Use Node 18 LTS for compatibility
  NODE_VERSION = "18"

